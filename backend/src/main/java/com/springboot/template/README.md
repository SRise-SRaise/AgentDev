# Legacy Template Code

该目录保留的是项目初始化阶段遗留的示例代码，当前主要是 `post` 相关的模板能力。

使用约定：
- 新业务不要继续放到 `template/` 下
- 登录、用户、文件、微信登录相关能力已经迁移到 `module/auth`、`module/file`、`model/*`、`mapper/system`
- 后续如果不再需要帖子示例代码，可整体迁移到 `legacy/` 或直接删除

当前保留本目录的原因：
- 避免一次性删除示例代码影响已有测试或参考实现
- 给后续队友提供过渡期参考
